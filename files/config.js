// Point this at your backend. For local development, leave as-is once
// the backend is running (see backend/README.md). After you deploy the
// backend (e.g. to Render/Railway), change this one line to that URL,
// e.g. "https://learn-and-sell-api.onrender.com/api".
window.LEARN_AND_SELL_API_BASE = "http://localhost:4000/api";
